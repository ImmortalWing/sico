# Sico：方向与设计目标

> 状态：方向草案（已确认部分）
> 更新日期：2026-09-04（所有者确认 application-ready language → Web/UI 与 Native Automation 分轨 → vision/model packages → application pilots 的应用层节奏；见 §3.1）

## 1. 名称

**Sico** 来源于 **Simple Coding**。

这里的 Simple 不表示功能简陋，而表示语言主动消除不必要的复杂性，让开发者和 AI 都能清楚地理解程序。

## 2. 一句话定位

Sico 是一门面向 AI 理解、生成、检查和修复代码的正规编程语言：让 AI 能快速把握程序功能与逻辑，尽量难以写出非法程序，并让编译器在出错时提供精确、稳定、可机器处理的诊断。

Sico 的核心关系是：

> AI 编写形式化代码，编译器约束 AI，人通过阅读、运行结果和自然语言反馈参与开发。

## 3. 背景判断

- Vibe Coding 主要用于创造新软件。
- Sico 首先是一门语言，不是应用框架或某种现有语言的包装器。
- 浏览器的传播优势不足以抵消 JavaScript 平台对语言语义和运行时的限制。
- Sico 当前不以浏览器为目标，优先建立独立、可移植的语言和运行时。
- TypeScript 最终仍执行 JavaScript 语义，因此不能作为绕开 JavaScript 的底层方案。

### 3.1 长期愿景

Sico 采用 **原生优先、生态先行、Web 后置** 的路线。

所有者于 2026-09-01 确认的产品目标：Sico 要成为一门**前端可用**的语言——覆盖 JavaScript 的大部分能力，包括网页控件与页面级 UI——后端能力同样要覆盖日常开发所需，并且自始至终便于 AI 使用。同日所有者进一步明确：**后端不仿照 Python 的模块清单**，而走 sico 自己的路径——分层精选的标准库（核心小而有证据、扩展能力走 versioned package 与包生态），是否与某门既有语言对表不作为成功标准。「Web 后置」是到达该目标的排序策略，不是对前端目标的放弃：§8.3 的阶段 6/7（Web 平台提案、浏览器一等支持）是通往前端目标的正式路径，其进入由原生生态的可验证结果决定。

所有者于 2026-09-04 进一步确认应用层节奏：安全 HTTP/API 自动化保持为独立基础能力，不与屏幕、窗口和输入控制混在同一里程碑；在 Web/UI 或原生视觉自动化之前，先建立“应用就绪语言基线”，用真实算法证明已接受源码语义能够构建和运行。之后 Web/UI 与 Native Automation Host 分轨，图像处理和模型推理进入 versioned package/provider 生态，最终由代表性 AI 应用和外部试点验收。语言无需等到永久不再演进，但普通应用逻辑不得依赖原生逃生舱。

长期目标不是破坏已有网页或要求浏览器删除 JavaScript，而是让浏览器为新应用提供不依赖 JavaScript 的一等执行路径，使开发者可以使用 Sico 完成从业务逻辑到系统能力调用的完整开发。

理想结果是：

- 新应用可以选择完全不使用 JavaScript；
- Sico 程序能够直接、安全地调用标准化宿主能力；
- 浏览器是否保留 JavaScript 不影响 Sico 应用运行；
- Sico 凭借真实用户、应用和包生态获得平台支持，而不是仅靠语言设计主张获得采用。

Sico 将 JavaScript 视为需要超越的既有平台，而不是需要兼容或复刻的语言。竞争目标是成为新软件的更好选择，不是破坏 Web 的向后兼容性。

## 4. 核心目标

### 4.1 降低 AI 产生非法代码的概率

语言应通过简单、统一、低歧义的结构，减少 AI 生成代码时常见的语法错误和结构错误。

重点包括：

- 一个概念尽量只有一种标准写法。
- 语法规则少而正交，避免大量特殊情况。
- 不依赖自动分号或隐蔽的空白规则。
- 避免容易混淆、需要长距离配对的结构。
- 不使用隐式类型转换制造隐藏语义。
- 官方格式化结果唯一，减少无意义的代码形态差异。
- 解析器能够局部恢复，单个错误不应产生大量级联错误。

### 4.2 让错误能够被准确修复

编译器不仅要判断代码是否有效，还要用最少的信息明确说明：

- 错误发生在哪里；
- 违反了哪一条语言规则；
- 编译器期望什么、实际得到了什么；
- 必要时给出一条直接、可执行的修复提示。

诊断信息必须清晰、简短，同时适合人类阅读和 AI 程序化处理。默认诊断不输出长篇解释；类型传播链、相关定义和更多修复候选通过按需查询获得。

### 4.3 降低 AI 理解程序的成本

Sico 不只优化“AI 能否写出可编译代码”，还要优化“AI 能否快速、准确地理解已有程序”。

语言和工具链应使 AI 无需通读整个仓库，就能回答：

- 一个模块提供什么功能；
- 一个函数需要什么、保证什么；
- 一段逻辑读取和修改哪些状态；
- 会产生哪些错误和副作用；
- 依赖哪些模块和平台能力；
- 修改一个符号会影响哪些调用者、测试和应用行为。

为此，Sico 追求：

- **语义局部性**：理解一个定义所需的信息尽量靠近定义；
- **显式数据流**：输入、输出、状态读写和错误路径可追踪；
- **显式副作用**：存储、网络、UI 和系统能力出现在类型或效果信息中；
- **形式化契约**：前置条件、后置条件和不变量可由编译器读取，而不是只写在自然语言注释中；
- **统一结构**：模块和定义具有规范顺序与唯一格式；
- **机器摘要**：编译器能够生成稳定、低 token 的模块概要、调用图、状态图和修改影响范围；
- **按需展开**：AI 先读取概要，只在需要时展开相关源码和依赖切片。

自然语言文档可以帮助解释业务目的，但不能成为程序正确性的唯一来源。类型、契约、效果、能力和状态变化才是 AI 理解逻辑的正式依据。

## 5. 源码的角色

Sico 不是纯自然语言编程。

- 自然语言用于人与 AI 交流需求和修改意见。
- Sico 源码是具有确定语法和确定语义的正式程序。
- AI 是 Sico 源码的主要编写者之一。
- 人应当能够阅读、审查和在需要时直接修改 Sico 源码。
- 相同源码在相同环境中必须具有可复现的含义，不能依赖模型临场解释。

## 6. 语言设计原则

### 6.1 强静态检查

Sico 采用强静态类型方向，在程序运行前尽可能发现结构和语义问题。

当前确认的原则包括：

- 支持局部类型推导，减少重复信息。
- 不进行隐式数字、字符串和布尔类型转换。
- `null` 不属于任意普通类型。
- 使用显式类型表达“可能不存在”和“可能失败”。
- 分支匹配必须完整覆盖所有可能情况。
- 变量默认不可变，变化必须显式表达。
- 函数的副作用应当能够被识别和检查。
- 网络、存储和系统能力应当受到显式能力约束。

### 6.2 确定且低歧义的语法

- 语法优先考虑稳定生成和可靠解析，而不是追求最少击键。
- 不照搬 JavaScript、Python 或其他语言的历史包袱。
- 语法应适合增量解析和局部修改。
- 编译器应产生稳定的 AST 或等价中间表示。
- 语言需要一种唯一、自动化的规范格式。

具体使用括号、缩进还是显式结束符，尚未确定，需要通过代表性程序比较后决定。

### 6.3 显式错误与副作用

- 可预期的失败不应依赖异常或隐藏控制流。
- 调用者应当能够从类型中看到函数是否可能失败。
- 对外部状态的读写应当可追踪。
- 权限不足、未处理错误和未覆盖状态应尽量成为编译错误。

### 6.4 语义局部性与可概括性

- 导入和外部依赖必须显式；
- 全局可变状态、隐式控制流和远距离语义影响应禁止或严格限制；
- 运算符重载、隐式派发、宏和元编程不得让实际调用目标无法静态确定；
- 公共函数应能从签名、契约、效果和错误类型概括主要行为；
- 状态转换应尽量使用封闭类型和显式转换规则表达；
- 编译器应建立符号、调用、数据流、效果、能力和测试之间的语义索引；正式 v0 身份、可信度、完整性和查询契约见 [`RFC-0002`](./docs/rfc/RFC-0002-semantic-index-query-v0.md) 与 [`semantic-index/`](./semantic-index/README.md)；
- 格式化器不仅统一空白，还应统一模块成员和契约信息的结构顺序。

具体契约语法、效果系统和状态机表达仍需通过代表性程序决定。

## 7. 编译器诊断协议

诊断协议以 **短、准、稳定、低 token** 为设计目标。每条默认诊断只包含：

- 稳定的错误编号；
- 文件与精确源码范围；
- 一句错误说明；
- 适用时的期望值与实际值；
- 至多一条首选修复提示。

例如：

```text
E2001 user.sico:18:10 expected User, found Option<User>
hint: handle none before using the value
```

机器模式表达相同信息，不重复自然语言段落：

```json
{
  "code": "E2001",
  "key": "TYPE_MISMATCH",
  "file": "user.sico",
  "range": {
    "start": { "byte": 240, "line": 18, "column": 10 },
    "end": { "byte": 252, "line": 18, "column": 22 }
  },
  "arguments": {
    "expected": "User",
    "found": "Option<User>"
  },
  "hint": "handle none before using the value"
}
```

诊断遵循以下规则：

- 一条诊断只报告一个主要问题。
- 优先报告根因，并抑制由其产生的级联错误。
- 相同错误必须产生相同编号和字段结构。
- 不在默认输出中复述源码或输出冗长解释。
- 详细类型链、相关定义和其他修复候选使用错误编号按需查询。
- 编译器支持限制错误数量，避免大量输出占用 AI 上下文。

错误编号和结构化字段属于工具协议，不能随意变化。未来的编辑器和 AI 工具应通过该协议调用编译器，而不是解析终端文本。正式 v0 契约、编号分区和兼容规则见 [`RFC-0001`](./docs/rfc/RFC-0001-diagnostics-protocol-v0.md)，机器目录见 [`diagnostics/`](./diagnostics/README.md)。

## 8. 实现语言与运行平台

### 8.1 实现语言

- 第一代 Sico 编译器使用 Rust 实现。
- 编译器前端、类型检查器、中间表示和核心运行时不依赖 JavaScript 或 TypeScript。
- Rust 是实现工具，不决定 Sico 的表层语法和语言语义。
- Sico 成熟后可以研究使用 Sico 实现自身编译器，但自举不是早期目标。

### 8.2 编译路径

Sico 定义自己的强类型中间表示，不把 C、C++、JavaScript 或 TypeScript 当作语义中间层。Sico IR 仅用于编译器内部分析和转换，不作为公开分发格式。

```text
Sico 源码
    ↓
AST 与类型检查
    ↓
Sico IR
    ↓
WebAssembly Core Module
    ↓
WebAssembly Component
    ├── .sapp 可移植应用包
    └── Component + Runtime 原生应用包
```

- WebAssembly Component 是 Sico 应用的首选正式执行与分发格式。
- Sico 不自研公开字节码、虚拟机、组件 ABI 或基础沙箱格式。
- 组件接口使用 WIT 描述，跨组件数据遵循 Component Model Canonical ABI。
- 通用系统能力优先复用 WASI；Sico 只定义 WASI 未覆盖的 UI、应用生命周期及平台能力接口。
- Sico Runtime 和 Sico Host 是原生程序，负责加载 Component 并提供经过授权的宿主接口。
- 独立原生应用早期可以通过“Component + 精简 Runtime”封装产生，不要求同时维护另一套机器码后端。
- 浏览器、DOM、HTML 和前端框架不属于当前目标。
- JavaScript 和 TypeScript 不进入编译器、核心运行时或正式后端。

### 8.3 平台发展阶段

Sico 按以下顺序发展，每一阶段必须拥有独立价值，不能依赖后续浏览器支持才能成立：

1. **语言基础**：确定语义、类型系统、诊断协议、中间表示和一致性测试。
2. **原生工具链**：使用 Rust 实现编译器，将 Sico 编译为 WebAssembly Component，并生成可移植 Sico 应用包。
3. **Sico 运行平台**：实现跨平台 Sico Runtime 和面向用户的 Sico Host，使同一个应用包可以在 Android 与桌面系统中打开。
4. **原生生态**：形成标准库、包管理、编辑器支持、测试工具及真实的命令行、服务端和桌面应用。
5. **可移植组件生态**：使用 WIT、Canonical ABI 和 WASI 建立稳定的能力接口与组件生态，但不依赖浏览器或 JavaScript。
6. **Web 平台提案**：基于已经验证的生态，为 Sico/Wasm 组件设计直接调用 DOM、网络、存储和事件能力的标准宿主接口。
7. **浏览器一等支持**：推动浏览器实现上述接口，使 Sico 应用无需 JavaScript 绑定层即可运行。

是否进入下一阶段由可验证结果决定，包括工具链稳定性、真实应用数量、包生态规模、跨平台一致性和外部采用情况。

### 8.4 Sico Runtime 与 Sico Host

Sico 可以先建立自己的跨平台应用宿主，而不等待浏览器支持。

所有者于 2026-09-01 确认了这条路线的理由：**Runtime/Host 的存在正是因为浏览器是否支持 Sico/Wasm 组件直跑不可控**——Runtime 是过渡期的正式宿主，让应用在浏览器支持到位之前就能获得完整能力；若阶段 6/7 成功，浏览器路径与 Runtime 路径并存，应用包语义不变，Runtime 不因浏览器支持而失效（桌面/Android 宿主仍需要它）。

暂定命名：

- **Sico Runtime**：使用 Rust 实现的执行、渲染、权限、存储和平台适配内核；
- **Sico Host**：安装在 Android、Windows、macOS 和 Linux 上，供用户打开、安装、授权、运行和管理 Sico 应用的产品；
- **`.sico`**：Sico 源代码文件；
- **`.sapp`**：经过编译、验证和封装的单文件 Sico 应用包。

基本流程：

```text
Sico 源码与资源
      ↓ sico build
单文件应用 example.sapp
      ↓ 打开文件或链接
Sico Host
      ↓
Sico Runtime
      ↓
原生窗口、图形、输入、网络、存储和系统能力
```

`.sapp` 是与 CPU 和操作系统尽量无关的可移植包，而不是某个平台的原生可执行文件。其正式可执行载荷采用 WebAssembly Component。它至少包含：

- 已通过类型检查的 WebAssembly Component；
- 应用清单、入口和版本；
- 图片、字体及其他资源；
- 所需能力声明；
- 内容哈希和签名信息。

Sico 不把内部 IR 放入公开应用包，也不再设计一套与 Component Model 重叠的通用字节码和 ABI。`.sapp` 负责应用级封装，Component 负责可执行代码与接口，二者职责分离。

Runtime v0 采用 Wasmtime：桌面使用 Cranelift，Android 先以 64-bit Pulley 实验路径验证。`.sapp` 只分发可验证的原始 Component，不把架构相关的 Wasmtime 预编译工件作为共享格式；完整理由见 [`ADR-0002`](docs/adr/ADR-0002-runtime-platform-baseline.md)。

Sico Host 在首次安装后，可以通过本地文件、下载链接或自定义协议打开 `.sapp`，形成类似“点击链接即浏览应用”的体验。应用也可以选择编译为独立原生安装包；可移植包与独立原生程序是两种发布形式，不改变 Sico 语言语义。

由于 `.sapp` 可以承载来自他人的可执行代码，Sico Runtime 必须从第一版就具备安全边界：

- 应用默认运行在沙箱中；
- 文件、网络、摄像头、剪贴板等能力显式声明和授权；
- 每个应用拥有隔离的存储空间和身份；
- 默认禁止任意原生 FFI 和不受控系统调用；
- 远程应用支持签名、哈希校验、资源限额和撤销机制；
- 编译器诊断、运行时权限错误和崩溃信息保持简短、结构化。

Sico Runtime 是语言之外的正式平台层。UI、渲染和应用生命周期不能混入核心语法；它们通过标准库、能力接口和独立 SDK 建立，以保证 Sico 仍然是一门通用语言而不是某个界面框架。

## 9. 当前非目标

现阶段不做以下事情：

- 不把 Sico 设计成自然语言提示词格式。
- 不把 Sico 设计成 HTML 或 JavaScript 的语法糖。
- 不先做 UI 组件库或 Web 框架。
- 不先选择 React、Vue 等具体技术栈。
- 不使用 TypeScript 作为 JavaScript 的替代运行时。
- 不为了浏览器兼容而引入 JavaScript 绑定层。
- 不重复发明 WebAssembly Component 已经提供的可移植字节码、Canonical ABI、组件组合与沙箱基础。
- 不为了快速演示而复制一套传统玩具语言语法。
- 不在编程模型和语义未确定前实现解释器或编译器。

## 10. 方向阶段的工作方法

在实际编写编译器之前，依次完成：

1. 确定 Sico 要防止的 AI 编程错误类别。
2. 设计若干具有代表性的完整程序，而不是孤立语法片段。
3. 对比多种语法结构，统计歧义、配对距离和修复难度。
4. 确定值、类型、函数、状态、错误和副作用的语义。
5. 定义 AST、中间表示及机器可读诊断协议。
6. 写出最小语言规范和一致性测试。
7. 方向稳定后，再实现编译器内核。

## 11. 判断 Sico 是否成功

Sico 不以语法是否新颖为成功标准，而看以下结果：

- AI 首次生成的代码能够通过语法解析的比例是否更高；
- 编译器是否能在运行前发现更多真实问题；
- AI 能否根据单轮结构化诊断完成正确修复；
- 完成一次正确修复所需的诊断 token 是否足够少；
- AI 正确概括模块功能所需读取的 token 是否足够少；
- AI 能否准确识别一次修改的依赖和影响范围；
- 修改一个需求时，代码变更是否局部且可预测；
- 人是否能够理解 AI 生成代码的结构和风险；
- 同一程序在不同工具和后端中是否保持一致语义。

## 12. 尚待决定

以下问题尚未形成结论：

- 具体语法外观与文件组织方式；
- 名义类型、结构类型或二者的组合；
- 泛型和多态模型；
- 所有权、生命周期和内存管理方式；
- 错误类型的具体表达；
- 副作用与能力系统的具体模型；
- 并发和异步语义；
- 模块、依赖与版本规则；
- 标准库边界；
- Android Pulley 能否满足真实应用性能、包体和商店发布门槛，以及何时允许可选 Cranelift；
- 采用哪个 WASI 基线版本，以及如何进行兼容升级；
- 是否在后期增加不经过 Component 的直接原生机器码后端；
- 图形界面和跨平台系统能力是否属于标准库。

这些内容必须经过示例和对比后确认，不能因为某门现有语言的习惯而直接继承。
