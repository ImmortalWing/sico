# M10 STEP-0097 handoff

> - status: complete; M10 in progress; STEP-0098 next
> - date: 2026-07-19
> - branch: `main`

## Current state

`sico-runner` owns a strict observed-run path. It verifies the debug Component/map/identity triplet, captures at most 256 typed Wasmtime frames, resolves only exact Component-file offsets and emits `sico.runtime-fault.v0`. JSON and text CLI output share that record. Missing coordinates are explicitly unavailable; stale sidecars fail as incompatible. Stream provider infrastructure failures are typed separately from guest-visible WIT errors.

The Runtime trace also corrected STEP-0096's embedded-module coordinate base and generated dispatcher gaps. STEP-0096 regression remains part of the validator.

## Next action

Execute STEP-0098 only:

1. freeze a portable cancellation-source enum and one terminal-winner state machine;
2. bridge Windows console Ctrl+C without inferring cancellation from process exit alone;
3. accept bounded typed cancellation requests for persistent watch/debug clients;
4. route timer, signal, client and Host cancellation through the same winner;
5. prove completion/cancel/timeout and double-cancel races emit exactly one terminal result;
6. prove busy guest and blocked Host operations observe the request and leave no worker/process residue.

Do not add execution-event transport or DAP yet. STEP-0098 may unlock documentation-only M11 STEP-0103 design, but scheduler implementation remains gated on M10 GO.

## Reproduction

```powershell
./tools/validate-step-0097.ps1
```

Expected summary:

```text
STEP_0097_OK classes=9 exact_source=true stale_refusal=true missing_map=unavailable frames=256/257-refused provider=typed cli=json+text engine_text_classification=false authority=unchanged next=STEP-0098
```
