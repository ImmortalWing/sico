# M10 STEP-0096 handoff

> - status: complete; M10 in progress; STEP-0097 next
> - date: 2026-07-19
> - branch: `main`

## Current state

`sico build --debug-info` now emits a deterministic, atomically installed Component/map/identity triplet. `sico-observability` verifies the complete non-circular digest chain, strict map coordinates and exact source document. Codegen records real absolute Core Wasm offsets for user operations/terminators and synthetic source-less rows for generated Script allocator/helper functions. Ordinary builds remain unlinked.

The STEP-0095 contract was clarified with mandatory `core_module` qualifiers because Script Components contain multiple Core modules. The change is reflected consistently in RFC-0035, JSON Schema, fixtures, PowerShell validator and Rust validator.

## Next action

Execute STEP-0097 only:

1. add strict `sico.runtime-fault.v0` production at the runner boundary;
2. load and verify the Component/map/identity triplet before source lookup;
3. walk at most 256 engine frames and resolve only exact `(core_module, function, offset)` mappings;
4. keep Host/provider frames distinct from guest source frames;
5. generate both text and JSON CLI presentation from the typed record;
6. test domain/trap/timeout/fuel/memory/malformed/Host failures plus stale/missing sidecars and adversarial recursion.

Do not add OS signal classification, execution event transport or DAP behavior yet. STEP-0097 must never parse engine prose to determine a stable fault class and must never produce a plausible source frame from a mismatched map.

## Reproduction

```powershell
./tools/validate-step-0096.ps1
```

Expected summary:

```text
STEP_0096_OK packages=4 deterministic_triplet=true maps=4/28 functions=1/8 synthetic=7 package_roundtrip=true module_packages=25 plain_debug_link=false authority=unchanged next=STEP-0097
```
