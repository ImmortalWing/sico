# M10 STEP-0098 handoff

> - status: complete; M10 in progress; STEP-0099 next; M11 STEP-0103 design unlocked
> - date: 2026-07-19
> - branch: `main`

## Current state

The native runner now owns a single terminal arbiter and typed cancellation sources. Real Windows console control, canonical client/debugger requests, timers, busy guest epochs and blocked Host polling all converge on that path. One-shot and watch modes both register scoped console guards. Client requests are bounded, canonical and run/generation matched.

## Next actions

Primary M10 lane — execute STEP-0099:

1. implement bounded `sico.execution-event.v0` production and decoding;
2. preserve run/generation/sequence plus reserved task/parent/scope/causal identities;
3. chunk binary stdout/stderr, enforce queue item/byte limits and deliver one terminal record after overflow;
4. run mandatory redaction before events cross the runner boundary;
5. cover one-shot, watch, REPL and debug lifecycle without introducing a second transport.

Unlocked M11 design lane — execute documentation-only STEP-0103 ADR:

1. decide one fresh Store with cooperative scheduler versus multiple Stores;
2. reconcile M8 scratch arena/post-return and affine resource rules across suspension;
3. freeze task/scope ownership, cancellation propagation and bounded queues;
4. do not implement scheduler code before M10 GO.

Do not start DAP before STEP-0099 events exist. Do not add TLS/provider work or capability authority.

## Reproduction

```powershell
./tools/validate-step-0098.ps1
```

Expected summary:

```text
STEP_0098_OK signal=windows-console-real client=one-shot+watch busy-read-pump-write-http=123 races=8/8 terminal=1 double-cancel=idempotent request=4096/4097-refused external-kill=not-cancelled authority=unchanged m11-adr=unlocked next=STEP-0099+STEP-0103
```
