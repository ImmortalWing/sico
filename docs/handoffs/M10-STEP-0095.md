# M10 STEP-0095 handoff

> - status: complete-contract; M10 in progress; STEP-0096 next
> - date: 2026-07-19
> - branch: `main`

## Current state

STEP-0095 accepted [`RFC-0035`](../rfc/RFC-0035-runtime-observability-debug-v0.md) and established [`observability/`](../../observability/README.md) as the machine source of truth. The validator proves three strict schemas, four protocol identities, seven positive/negative fixtures, 38 exact DAP rows, seven cancellation cases, eight both-order races and two distinct deterministic real Components.

No compiler debug-map emission, Runtime frame/event producer, signal bridge or DAP adapter exists yet. `sico.debug` must continue to refuse.

## Next action

Execute STEP-0096 only:

1. define canonical byte serialization for `sico.debug-map.v0`;
2. emit a compact `sico.debug-link.v0` Component custom section containing the sidecar digest binding;
3. implement the accepted non-circular chain: pre-debug Component code hash → map digest → link section → final Component digest;
4. integrate deterministic build/cache/package handling;
5. prove repeated builds, representative mapping coverage, malformed/stale/limit+1 rejection and debug-disabled honesty.

Do not add Runtime trap-to-source lookup, Ctrl+C classification, execution events or DAP behavior in STEP-0096. Compiler crates must not depend on runner, DAP or platform crates.

## Reproduction

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools/validate-step-0095.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File tools/validate-m0-exit.ps1
```

Expected contract summary begins:

```text
STEP_0095_OK schemas=3 identities=4 fixtures=7 accepted=1 rejected=6 dap_supported_requests=12 dap_refused_requests=20 dap_events=6 cancellation_cases=7 races=8 components=2 deterministic=true
```
