# M9 STEP-0092 接手文档

> - 更新时间：2026-07-19
> - 分支：`main`
> - 步骤状态：STEP-0092，`complete`
> - 下一步骤：STEP-0093，`planned`

## 一句话状态

STEP-0092 已接受 explicit typed `main`，明确拒绝 unrestricted top-level statements 与 `script:` block，并用 E1013 修复了两者被静默丢弃却通过 `check` 的缺陷。

## 冻结边界

- top level 只允许 declarations；Script entry 仍是普通显式 `main`。
- Candidate B/C 在 parser 阶段产生 `E1013 / SYNTAX_UNEXPECTED_TOP_LEVEL`。
- `script:` rejection 只产生一个根诊断，并恢复到 `end script` 或下一 declaration。
- formatter 不输出、HIR/semantics 不运行、LSP/AI inspect 返回同一 identity。
- `script` 不成为新 keyword；无 module initializer、hidden capability 或 cache identity 变化。

## 下一项

STEP-0093 完成 editor/debug/AI execution integration：统一 run/watch/REPL 协议与 cancellation/source-map/log bounds，并证明所有工具都直接传参、无 shell interpolation。
