// case: STREAM-002
// syntax: C
// expect: accept
// semantics: SEM-093, SEM-105

enum ReadError {
  Failed,
}

async fn collect_small(stream: Stream[Int, ReadError]) -> Result[List[Int], ReadError] {
  using stream {
    return await stream.collect(limit: 100)
  }
}
