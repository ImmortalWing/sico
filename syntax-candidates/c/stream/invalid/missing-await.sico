// case: STREAM-102
// syntax: C
// expect: reject(ASYNC_VALUE_REQUIRES_AWAIT)
// semantics: SEM-100, SEM-105

enum ReadError {
  Failed,
}

fn pull_one(stream: Stream[Int, ReadError]) -> Result[Option[Int], ReadError] {
  using stream {
    return stream.next()
  }
}
