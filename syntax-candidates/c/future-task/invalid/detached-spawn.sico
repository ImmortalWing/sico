// case: TASK-104
// syntax: C
// expect: reject(TASK_DETACHED)
// semantics: SEM-101, SEM-102

async fn compute() -> Int {
  return 1
}

async fn detached() -> Int {
  let handle = spawn compute()
  return await handle
}
