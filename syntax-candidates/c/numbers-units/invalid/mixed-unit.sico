// case: NUM-103
// syntax: C
// expect: reject(TYPE_MISMATCH)
// semantics: SEM-003, SEM-032

opaque Bytes = Int
opaque Seconds = Int

fn set_limit(limit: Bytes) -> Unit {
  return Unit
}

fn main() -> Unit {
  return set_limit(Seconds(5))
}
