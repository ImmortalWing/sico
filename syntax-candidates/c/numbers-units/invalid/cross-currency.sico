// case: NUM-104
// syntax: C
// expect: reject(TYPE_MISMATCH)
// semantics: SEM-003, SEM-032

opaque Cny = Int
opaque Usd = Int

fn charge(amount: Cny) -> Unit {
  return Unit
}

fn main() -> Unit {
  return charge(Usd(100))
}
