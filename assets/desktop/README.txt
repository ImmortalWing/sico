Sico Desktop Host M5 Windows smoke package

Use association-plan to inspect per-user Open With registration.
Use association-apply only when you explicitly want to register .sapp for this user.
Persistent package installation requires a locally trusted development public key.
