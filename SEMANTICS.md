# Sico 核心语义

> - 版本：0.1-draft
> - 状态：M0 核心语义草案
> - 依据：10 个代表性程序与跨样本问题矩阵
> - 范围：定义程序的含义，不定稿表层语法

## 1. 文档目的

本文档定义 Sico 程序在不同编译器、Runtime 和平台上必须保持一致的核心语义。它先回答“程序表示什么”，再由候选 B、C 回答“程序应当怎样写”。

以下内容不是候选 A 源码的语法说明。示例中的 `use`、`with`、`end`、`try`、`async` 等形式仍可替换；编译器实现不得通过先写解析器的方式替语言设计做决定。

本文档使用三种状态：

| 状态 | 含义 |
|---|---|
| **确认** | 已由项目方向确定，后续修改需要明确的设计决策 |
| **草案** | 当前首选语义，必须用全部代表性程序和候选语法验证 |
| **开放** | 证据不足，不能由编译器实现隐式决定 |

语义条目使用稳定编号。编号用于测试、诊断、RFC 和 AI 工具引用；即使条目被替代，也不复用其编号。

## 2. 语义总原则

### SEM-001：唯一正式含义【确认】

合法 Sico 程序具有由语言版本确定的唯一含义。格式化、编译优化、Wasm 降低和 Runtime 选择不能改变可观察行为。

### SEM-002：安全程序没有未定义行为【确认】

安全 Sico 程序的每个操作必须产生值、显式失败、取消或受控终止之一。语言不允许读取未初始化内存、越界后继续执行、数据竞争或依赖宿主内存布局。

### SEM-003：无隐式类型转换【确认】

数字、布尔、文本、枚举、领域类型和可选值之间不存在隐式转换。转换必须在源码或生成的接口适配层中可见，并具有确定的失败语义。

### SEM-004：求值顺序确定【草案】

单个任务内使用从左到右求值：先求值调用目标，再按源码顺序求值参数，最后执行调用。布尔短路操作只求值必要一侧。

编译器可以重排纯计算，但不能改变错误、资源、效果或终止的可观察顺序。

### SEM-005：默认不可变【确认】

绑定、记录、集合和枚举值默认不可变。状态变化产生新值；局部可变必须显式声明，且不能通过别名形成不可见的共享修改。

### SEM-006：边界显式【确认】

公共函数、组件接口和平台调用必须显式暴露输入、输出、预期错误、效果与所需能力。类型推导不得跨越这些边界隐藏事实。

## 3. 程序和名称模型

### SEM-010：词法作用域【草案】

名称采用词法作用域。内层定义可以引用可见的外层名称，但局部名称不能泄漏到作用域之外。

同一作用域中禁止重复定义同名值、类型或模块别名。是否允许值名称和类型名称相同属于表层名称空间设计，不改变解析后的唯一绑定要求。

### SEM-011：引用在编译期绑定【确认】

普通名称引用在编译期解析为唯一符号。核心语言没有根据字符串动态查找函数、字段或类型的机制。反射若在未来加入，只能作为受能力约束的平台 API。

### SEM-012：导入不授予权限【确认】

模块导入只使名称可见，不授予文件、网络、存储、时钟、UI 或组件加载权限。依赖关系与能力关系是两张不同的图。

### SEM-013：初始化不得隐藏执行【草案】

模块顶层只允许声明和可在编译期求值的常量。导入模块不会执行任意用户代码，程序启动顺序不依赖导入顺序。

## 4. 值与类型

### 4.1 类型分类

### SEM-020：值类型与资源类型分离【草案】

所有类型分为两类：

- **值类型**：具有值语义，可以在不改变程序含义的前提下复制、共享或内联实现；
- **资源类型**：表示文件、流、网络响应、任务、组件实例等具有身份和生命周期的对象，默认不可复制。

类型分类是公开语义。后端不能把资源伪装成普通整数句柄，也不能让用户观察值类型的内存地址或共享计数。

### SEM-021：核心值类型【草案】

最小核心包含：

- `Unit`：唯一值，用于没有业务返回值的计算；
- `Never`：没有任何值，用于不会正常返回的控制流；
- `Bool`：只有 `true` 和 `false`，不存在 truthy/falsy；
- `Int`：任意精度有符号整数，普通整数计算不溢出；
- `Float32`、`Float64`：明确宽度的 IEEE 754 浮点数；
- `Char`：一个 Unicode 标量值；
- `Text`：合法 Unicode 文本；
- 记录、封闭枚举、元组、函数值以及核心泛型类型。

固定宽度整数属于核心接口类型，但名称和表层暴露方式仍开放。它们主要用于二进制、WIT 和系统边界；窄化转换必须显式并可能失败。

`Decimal`、`Money`、`Path`、`Url`、`Duration`、`Size` 和日期时间不是编译器内建标量。它们由标准库提供强类型语义，其中 `Money` 应由应用进一步封装币种或领域约束。

`Int` 采用任意精度是当前草案选择，需要通过性能原型验证；如果被固定宽度整数替代，必须重新设计普通算术的溢出语义。

### SEM-022：没有通用 `null`【确认】

普通类型不包含 `null`、`undefined`、未初始化值或隐式缺失状态。可能不存在使用 `Option<T>` 表达，可能失败使用 `Result<T, E>` 表达。

### SEM-023：文本边界明确【草案】

`Text` 在语义上是不可变 Unicode 文本，外部字节进入 `Text` 时必须验证编码。语言不提供含义模糊的 `text[index]`：

- 字节访问使用显式 UTF-8/二进制 API；
- Unicode 标量遍历使用显式 scalar API；
- 用户可见字符处理使用显式 grapheme API；
- 单词划分使用带版本的 Unicode 文本库规则。

字节数、标量数和字素簇数使用不同操作名称，不能共用一个无单位的 `length`。

### SEM-024：浮点行为可识别【草案】

浮点数不与整数或 Decimal 隐式转换。NaN、无穷和负零遵循明确宽度的 IEEE 754 语义；涉及精确金额、计数和标识的数据不得依赖浮点近似相等。

跨后端必须保持已规定的浮点结果；允许平台差异的数学函数必须在标准库接口中单独标记，不能冒充完全确定的纯函数。

### 4.2 复合类型

### SEM-030：记录是名义类型【草案】

具名记录声明创建新的名义类型。两个记录即使字段完全相同，也不能自动互换。字段名称和类型是该记录 schema 的一部分。

记录构造必须覆盖全部无默认值字段，禁止未知字段。记录更新产生新值，未更新字段保留原值；不能通过字段赋值原地修改已绑定记录。

公共记录的字段增删和类型变化属于接口兼容性问题，不能只按内部内存布局处理。

### SEM-031：枚举是封闭代数数据类型【草案】

枚举由有限个具名变体组成，变体可以携带具名数据。普通枚举在其语言版本内封闭，匹配必须覆盖所有可达变体。

新增变体是语义影响事件。编译器必须把受影响的匹配、序列化接口、组件接口和测试加入影响分析。

跨版本确需前向兼容的接口应显式声明非穷尽策略，或提供 `Unknown` 数据变体；不能用通配分支默默吞掉未来状态。

### SEM-032：领域新类型是名义且不透明的【草案】

领域新类型创建与底层表示不同的类型。其值只能通过公开构造和转换获得，不能因底层类型相同而互换。

编译器可以使用零成本表示，但该优化不可观察。领域新类型的验证规则、单位和安全属性必须进入语义索引。

### SEM-033：元组仅表达局部位置关系【草案】

元组是按位置组成的值，适合短小、局部、一次性的组合。公共 API、持久化 schema、错误载荷和 Component 接口应优先使用具名记录；候选语法评测将验证是否需要进一步限制元组。

### SEM-034：Option 与 Result 是普通但受识别的封闭类型【草案】

核心语义等价于：

```text
Option<T> = Some(value: T) | None
Result<T, E> = Ok(value: T) | Error(error: E)
```

它们不依赖特殊运行时对象。编译器可以为检查、传播、布局和诊断进行专门处理，但不能改变其分支语义。

### SEM-035：集合具有确定行为【草案】

标准集合至少包括 `List<T>`、`Map<K, V>` 和 `Set<T>`：

- `List` 是保持顺序的不可变值序列；
- `Map` 和 `Set` 的遍历顺序必须由具体类型明确，不得依赖随机哈希或后端实现；
- 更新返回新集合，允许编译器使用结构共享；
- 元素相等、键约束和序列化顺序必须从类型与接口中可知。

是否提供独立的插入有序、排序和哈希集合类型仍开放；确定性要求不开放。

### 4.3 类型关系与推导

### SEM-040：无隐式子类型替换【草案】

核心名义数据类型之间没有继承和隐式子类型转换。复用通过组合、泛型约束和显式接口完成。`Never` 可以出现在任何预期值位置，除此之外的类型兼容必须可静态说明。

### SEM-041：泛型保持参数类型【草案】

泛型是静态参数多态。类型参数在公共签名中显式出现，编译器不能通过运行时装箱把类型错误推迟到执行期。

泛型约束、接口/trait 模型、方差和代码生成策略仍开放，但必须满足：同一实例化具有唯一类型，错误信息能指出约束来源，Component 边界只暴露 WIT 可表示的具体类型。

### SEM-042：推导只在局部减少重复【确认】

允许从初始化值、调用参数和局部分支推导局部绑定类型。以下位置必须写出或由接口文件直接给出完整类型：

- 公共、导出和递归函数签名；
- 记录字段与枚举载荷；
- 模块和 Component 边界；
- 持久化 schema；
- 无法从单一局部表达式确定的空集合和缺失值。

编译器不得通过分析远处调用者来决定一个公开定义的类型。

### SEM-043：值相等与资源身份分离【草案】

只有声明支持相等的值类型可以比较。记录和枚举的相等由其组成值递归决定；函数和资源默认不可比较。

资源身份只能通过其类型提供的明确操作观察，不能使用通用地址相等。排序和哈希也必须由类型明确支持，不从内存表示推导。

## 5. 绑定、状态与内存

### SEM-050：绑定默认不可重新赋值【确认】

普通局部绑定在初始化后不能重新赋值。显式可变绑定仅允许改变该局部槽位，不会使已产生的旧值随之变化。

### SEM-051：不允许隐式共享可变状态【草案】

闭包、任务和集合不能捕获一个可写局部别名并在不可见位置修改它。跨任务共享状态必须通过具有明确同步和效果语义的资源类型完成。

模块级可变变量不进入最小核心。持久状态、UI 状态和缓存通过显式值传递或能力资源管理。

### SEM-052：值的内存管理不可观察【草案】

值由语言自动管理。实现可以栈分配、复制、引用计数、追踪回收或结构共享，只要析构时机不会成为值类型的可观察行为。

资源释放时机由资源语义确定，不依赖垃圾回收。闭包循环、跨异步值保存和大值复制策略需要实现原型，但不改变值语义。

## 6. 函数与控制流

### SEM-060：函数签名概括行为【确认】

函数的完整语义签名至少包含：参数类型、返回类型、预期错误、同步/异步性质、效果集合和能力要求。表层语法可以省略允许局部推导的部分，语义模型不能省略。

### SEM-061：参数按值传递【草案】

值参数按值语义传递；实现可共享不可变表示。资源参数默认移动所有权，除非类型声明为受限借用或共享资源。

参数求值和移动顺序必须明确。使用已经移动的资源是编译错误。

### SEM-062：闭包捕获显式进入语义【草案】

函数值可以捕获不可变值。捕获资源需要显式移动，捕获能力会把相应能力和效果要求加入函数值类型及语义索引。

闭包不能把局部借用带出其有效作用域。是否在表层语法中标记捕获列表仍开放。

### SEM-063：控制条件必须是 Bool【确认】

条件语句和 guard 只接受 `Bool`。数字、文本、Option、集合和资源没有隐式真假语义。

### SEM-064：匹配必须完整且不可重复命中【草案】

`match` 按源码顺序选择第一个满足模式和 guard 的分支，但编译器必须检查：

- 对封闭类型覆盖完整；
- 不可达或被前序完全遮蔽的分支；
- 每个分支绑定的名称和类型；
- 所有产生值的分支结果类型一致。

对封闭枚举使用通配分支默认是编译错误，因为它会隐藏新增变体。忽略载荷与忽略整个未来分支是不同操作。

### SEM-065：非 Unit 函数不能隐式越过末尾【草案】

返回 `Unit` 的函数可以正常到达末尾。其他函数的每条可达路径必须明确产生返回值、传播错误或终止。候选语法可以比较“显式 return”和“块值”，但不能允许遗漏返回静默产生默认值。

### SEM-066：循环必须具有可分析出口【草案】

循环不产生隐式值。无限循环、提前退出和带值退出必须在语义图中区分。集合迭代不得暴露会随遍历发生不可预测变化的同一集合。

## 7. 错误和终止

Sico 把计算结果分成以下层次，工具和 Runtime 不得把它们压成同一个字符串错误：

| 层次 | 表达 | 调用者能否按普通控制流处理 |
|---|---|---|
| 正常值 | `T`，其中可以包含 `Option` | 可以 |
| 预期失败 | `Result<T, E>` | 可以，且必须处理或传播 |
| 取消 | 任务控制结果 | 只在结构化并发边界观察 |
| Trap/隔离故障 | Component 或隔离单元终止 | 只能由外层 Runtime/组件调用边界处理 |
| Runtime 致命故障 | Runtime 无法继续安全执行 | 不可由应用恢复 |

### SEM-070：预期失败使用 Result【确认】

输入无效、业务拒绝、记录不存在、普通 I/O 失败和可恢复的平台失败都属于预期失败，使用 `Result<T, E>` 返回。核心语言没有可被任意抛出和捕获的异常机制。

错误类型应是具名封闭枚举或领域新类型。字符串只用于面向用户的附加信息，不能代替机器可判定的错误变体。

### SEM-071：错误传播不做隐式类型转换【草案】

传播操作只能直接传播当前函数已经声明的同一错误类型。不同错误类型必须使用显式映射，映射需要覆盖所有来源错误。

编译器不得根据名称相似、字符串内容或隐式 `From` 规则改变错误类型。这样 AI 可以从局部源码确定每条错误路径。

### SEM-072：取消不是业务错误【草案】

取消是异步控制结果，不自动塞入每个业务错误枚举。取消会沿任务树传播并执行资源清理；只在结构化并发边界转换为可观察的任务结果。

应用若要把取消显示为领域状态，必须在允许观察取消的边界进行显式映射。

### SEM-073：超时是由时钟操作产生的预期结果【草案】

超时不是自动发生的语言异常。带时限操作需要时钟能力，并返回可识别的超时结果。测试可以注入虚拟时钟，超时不会依赖不可替换的墙上时间。

### SEM-074：Trap 与业务失败隔离【确认】

越界、损坏的 Component、资源限额终止和不可恢复的 Runtime 故障属于 trap/宿主故障，不伪装成业务错误。Runtime 在 Component 隔离边界捕获并报告它们；组件内部不能用普通 `Result` 捕获任意 trap 后继续执行。

调用不可信组件的 API 必须让调用者区分组件返回的领域错误、取消/超时和组件 trap。

### SEM-075：契约违反是程序缺陷【草案】

未被静态证明且运行时失败的契约检查产生受控的 contract violation，终止当前隔离单元，不作为普通业务错误恢复。诊断必须包含契约编号和最小相关值，不能泄漏敏感宿主信息。

## 8. 效果与能力

### SEM-080：效果与能力是不同概念【确认】

- **效果**描述计算会做什么，例如读取存储、写控制台、发起网络请求或启动任务；
- **能力**描述计算被授予对哪个宿主资源执行某类操作的权限。

纯函数没有外部效果，也不依赖能力。读取本地不可变值和构造新值不是外部效果。

### SEM-081：效果沿调用图传播【草案】

私有函数的效果可以局部推导；公共、导出和 Component 边界必须具有完整、稳定的效果集合。调用者的效果集合至少包含被调用者实际执行的效果，除非某个效果被明确的 handler 消除或转换。

动态分派和函数值调用的效果上界必须存在于其静态类型中，不能等运行时才发现。

### SEM-082：能力以不可伪造资源表示【草案】

能力由 Runtime 提供的类型化资源值或 Component import 表示。用户代码不能从文本、整数、路径或模块名称构造能力。

能力可以被缩小、委托或移动，但不能自行扩大。能力资源本身不等同于 manifest 中的权限声明。

### SEM-083：权限取交集【确认】

应用实际获得的能力是以下集合的交集：

1. 源码和 Component imports 能够请求的能力；
2. `.sapp` manifest 声明的能力上界；
3. 用户或管理员授予的权限；
4. Runtime 当前平台和策略实际提供的能力。

任一层缺失都不能获得权限。构建时必须验证源码语义、生成的 Component imports 和 manifest 一致。

### SEM-084：能力拒绝返回去敏错误【草案】

能力缺失、范围不符或被撤销时返回结构化宿主错误。默认错误只说明能力类别和拒绝原因，不暴露真实主机路径、账户身份、令牌或系统内部对象。

## 9. 资源生命周期

### SEM-090：资源默认是 affine【草案】

资源值至多被消费一次：可以移动或提前关闭，但不能隐式复制。离开拥有者作用域时，尚未关闭的资源必须确定性释放。

显式关闭后的再次使用、使用已移动资源、让借用逃逸和并发无保护访问都是编译错误。

### SEM-091：所有退出路径都清理资源【确认】

正常返回、Result 错误传播、提前退出、取消和受控终止都执行已经登记的资源清理。Runtime 强制终止整个隔离单元时至少回收宿主资源，但不能承诺继续执行用户清理代码。

### SEM-092：资源跨异步和组件边界需要类型许可【草案】

资源只有在其类型明确声明可转移或可共享时才能跨任务。资源跨 Component 边界使用 Canonical ABI resource handle，不复制宿主对象。

普通值的泛型约束不能自动证明资源安全；资源约束和生命周期必须进入类型检查及语义索引。

### SEM-093：资源限额属于 Runtime policy【草案】

内存、CPU、文件数、响应体大小、任务数和执行时间的硬上限由 Runtime policy 执行。源码/API 可以请求更小限额，manifest 可以声明上界，但应用不能扩大 Runtime 上限。

达到可恢复的操作限额时返回结构化错误；威胁隔离或无法安全恢复时终止当前组件实例。

## 10. 异步、并发与流

### SEM-100：异步性质属于函数类型【草案】

可能挂起的函数在类型中与同步函数不同。一个返回 `T` 的异步函数在调用后产生 `Future<T>`；如果计算可能产生预期失败，则 `T` 本身是 `Result<V, E>`，不再创建第二条隐藏错误通道。

Future 是只能完成一次的 affine 资源。只有显式等待或启动任务才推进异步计算；等待取得它的普通返回值，取消和 trap 按本章其他条目处理。具体关键字仍由候选语法决定。

隐藏在普通同步调用中的阻塞不是可接受实现。Runtime 和宿主接口必须公开可能挂起的边界。

### SEM-101：并发必须显式启动【草案】

创建 Future 不等于创建并发任务。并发执行需要显式 spawn/任务组操作，因此源码和语义图可以识别所有并发分支。

调度器可以用任意合法顺序推进任务；程序若依赖完成顺序，必须显式选择并记录该策略。

### SEM-102：结构化并发是默认模型【草案】

每个子任务属于一个可见任务作用域。作用域正常结束前必须完成所有子任务；作用域因错误或取消退出时，未完成子任务被取消并完成清理。

可逃逸的后台任务不属于最小核心。平台服务若需要长期任务，必须通过具有生命周期和关闭语义的资源 API 提供。

### SEM-103：任务句柄是资源【草案】

任务句柄不可隐式复制或丢弃。等待任务会得到 `Completed(value: T)` 或 `Cancelled`；当 `T` 是 `Result<V, E>` 时，领域失败仍在该普通返回值内。任务内部 trap 按隔离边界报告，不映射为任意业务错误。

父任务取消向子任务传播。子任务失败是否取消同组其他任务由显式的 fail-fast 或 collect-all 策略决定。

### SEM-104：并发不承诺结果顺序【确认】

并发完成顺序不是输入顺序。需要输入顺序、完成顺序或稳定键顺序时，调用者必须明确选择；标准库 API 不得使用含义模糊的 `all()` 默认值隐藏该决定。

### SEM-105：Stream 是带背压的异步资源【草案】

`Stream<T, E>` 表示按需产生零到多个值、可能失败且可取消的资源。其单步语义等价于返回 `Future<Result<Option<T>, E>>`：`Some` 是下一个值，`None` 是正常结束，`Error` 是预期流错误，取消仍是独立控制结果。

消费者拉取下一个值形成背压；收集为 List 必须显式处理内存或数量上限。

Stream 与 List 可以共享 `map`、`filter` 等概念，但返回类型、是否异步、错误和资源生命周期保持可见，不能用一套动态 API 混淆。

### SEM-106：并发程序保持数据竞争安全【确认】

不可变值可以安全共享。可变状态只能由单一任务拥有，或封装在声明同步语义的资源中。安全 Sico 不允许无同步的数据竞争。

Sico 异步模型将优先映射到 WebAssembly Component 的标准 async/future/stream 能力，但语言语义不绑定某个尚未稳定的 ABI 版本。

## 11. 契约、状态机与事务

### SEM-110：契约是机器可检查的语义【草案】

契约包含前置条件、后置条件和类型/状态不变量。契约表达式必须纯、确定、终止且不能读取未声明的外部状态。

编译器尽可能静态证明契约；未被证明的可执行契约在规定边界运行时检查。优化只能删除已证明恒真的检查，不能静默关闭契约。

契约、证明状态和依赖符号必须进入语义索引，供 `describe`、`impact` 和测试选择使用。

### SEM-111：状态机先使用封闭数据与纯转换【草案】

最小核心不新增专用状态机运行机制。状态使用封闭枚举/记录表达，事件转换使用完整匹配的纯函数表达，非法转换返回具名错误。

编译器从类型、匹配和契约派生状态图与拒绝路径。如果普通数据类型无法可靠表达更复杂的并发状态机，再根据新样本提出语言扩展。

### SEM-112：事务不是任意语言块【草案】

事务由存储或其他能力资源提供，其原子性范围属于该资源接口契约。最小核心不把任意效果块标记成自动事务，也不承诺跨不同 Component、网络或存储提供者的分布式原子性。

同一提供者可以暴露类型化 transaction resource/callback API；事务内允许的效果、异步和重试行为必须由接口明确。业务函数应与提交操作分离，以便测试和重试。

### SEM-113：并发更新使用显式版本【草案】

需要防止旧结果覆盖新状态时，持久化与 UI API 使用 revision/version token。提交时不匹配返回具名 stale/conflict 错误；Runtime 不进行不可见的最后写入获胜。

## 12. 模块、Component 与平台边界

### SEM-120：语言核心不复制平台功能【确认】

文件、网络、时钟、随机、存储、UI、应用生命周期和动态组件加载不属于核心语言语法。它们通过标准库、WASI 或 Sico WIT 平台接口提供，并受效果与能力检查。

### SEM-121：Sico interface 映射到 WIT【草案】

可导出的 Sico interface 是 WIT Component 接口的语言级映射，不定义第二套 ABI。只有 WIT 可表示且满足资源规则的类型可以直接跨 Component 边界。

编译器负责生成 WIT 与绑定、验证名称和类型可表示性，并保留错误、资源和异步语义。适配器负责版本转换，不修改组件内部 Sico 语义。

### SEM-122：组件版本不属于泛型类型参数【草案】

接口版本属于包和接口身份元数据，不与语言泛型共用一套 `<...>` 语义。版本选择、兼容范围和 adapter 由包解析与 Component 组合阶段处理。

### SEM-123：动态组件加载属于平台能力【草案】

静态组合优先，因为它允许完整调用图、权限闭合和影响分析。动态加载必须通过受能力约束的 Component loader，显式给出允许接口、能力上限、资源限额和故障映射。

动态插件不能获得宿主未授予给加载器的能力，也不能通过声明新 import 扩大权限。

### SEM-124：UI 是 SDK，不是语言核心【确认】

Model/Event/Effect、视图树、控件、布局和生命周期可以由标准 SDK 建模，但当前不进入核心语义。UI 回调仍遵守普通函数、效果、能力、资源、异步和 revision 规则。

## 13. AI 语义模型

### SEM-130：编译器生成可信语义索引【确认】

编译器在成功或部分成功检查后生成机器可读索引，至少记录：

- 符号与稳定标识；
- 完整类型签名；
- 调用与被调用关系；
- 值和资源的数据流；
- Result 错误传播与显式映射；
- 效果和能力传播；
- 状态转换、拒绝路径和完整性；
- 契约、不变量与证明状态；
- Component import/export；
- 关联测试与源码范围。

索引来自已解析和已检查的程序，不把注释或模型猜测当作事实。

### SEM-131：自然语言目的与正式事实分离【确认】

模块、类型和函数可以包含 purpose/文档说明，但工具输出必须标记：

- `declared`：作者声明的目的；
- `verified`：由类型、控制流、效果和契约证明的事实；
- `inferred`：工具基于结构得到、尚未证明的描述。

AI 不应把 `declared` 或 `inferred` 内容冒充编译器保证。

### SEM-132：查询协议按最小充分上下文返回【草案】

`outline`、`describe`、`slice`、`impact` 和 `flow` 共享同一语义 ID 和 JSON 类型。默认只返回回答问题所需的信息，并提供继续查询的引用，不重复整个模块源码。

程序切片必须保留目标行为所需的类型、错误、效果、能力、契约、状态和 Component 边界；压缩不能删除改变结论的分支。

### SEM-133：不完整程序仍产生有界信息【草案】

存在语法或类型错误时，编译器可以输出部分索引，但每个事实必须标记完整性和阻塞诊断。错误节点不能被伪装成确定调用或确定类型。

## 14. 与后端和 Runtime 的关系

### SEM-140：Sico IR 保留核心语义【确认】

降低到 Sico IR 时必须仍可表示：名义类型、完整匹配、Result 路径、效果与能力、资源移动和清理、异步任务、契约来源以及源码跨度。

如果某项语义只能靠后端约定而无法在 IR 验证器中检查，说明 IR 设计不完整。

### SEM-141：Wasm 是实现目标，不是语言语义【确认】

Wasm 数值宽度、线性内存、Canonical ABI 布局和 Runtime 引擎行为不能反向泄漏为 Sico 的默认值语义。无法直接映射的 Sico 值由编译器和运行时库实现。

### SEM-142：跨平台结果一致【确认】

在相同显式输入、能力响应和调度选择下，Windows、macOS、Linux 和 Android 上的核心值、匹配、错误、文本和资源规则一致。平台差异必须通过接口数据或能力可见。

## 15. 对诊断的直接要求

每条核心语义都必须对应可测试的静态检查或运行时边界。默认诊断保持短小，并提供稳定编号与按需详情。

首批语义诊断至少覆盖：

| 类别 | 必须发现的问题 |
|---|---|
| 类型 | 隐式转换、错误领域类型、窄化失败未处理、边界类型缺失 |
| 匹配 | 缺失变体、不可达分支、封闭枚举通配隐藏 |
| 错误 | Result 未处理、错误类型隐式变化、trap 被当作业务错误 |
| 状态 | 修改不可变绑定、旧 revision、契约可能被破坏 |
| 效果 | 公共效果声明不足、纯函数调用有副作用函数 |
| 能力 | 能力来源不存在、manifest/import/源码不一致 |
| 资源 | 重复使用、移动后使用、未确定释放、非法跨任务 |
| 异步 | 未等待 Future、任务逃逸、取消路径未清理、顺序策略缺失 |

诊断修复建议不能偷偷改变业务含义。例如缺失 enum 分支时，编译器不得默认建议添加吞掉所有未来状态的 `_` 分支。

## 16. 决策覆盖矩阵

| 问题组 | 本文状态 | 主要条目 |
|---|---|---|
| T01-T10 类型与数据 | 已形成草案；泛型约束仍开放 | SEM-020—043 |
| C01-C04 控制流 | 已形成草案；表层块语法仍开放 | SEM-063—066 |
| E01-E04 错误 | 已形成草案 | SEM-070—075 |
| S01-S03 状态 | 已形成草案 | SEM-050—052、111 |
| K01-K02 契约 | 已形成草案；验证技术仍开放 | SEM-110、075 |
| X01-X02 事务 | 已确定边界草案 | SEM-112—113 |
| FX01-FX03 效果能力 | 已形成草案；效果粒度仍开放 | SEM-080—084 |
| R01-R04 资源 | 已形成草案 | SEM-090—093 |
| A01-A07 异步 | 已形成草案；ABI 映射仍开放 | SEM-100—106 |
| P01-P07 Component | 已确定边界草案 | SEM-120—123 |
| UI01-UI06 UI | 确认留在 SDK/平台层 | SEM-124 |
| AI01-AI07 AI 协议 | 已形成语义要求；JSON schema 待定义 | SEM-130—133 |

## 17. 仍需单独决策的问题

以下问题不能通过本文首版直接视为完成：

1. `Int` 任意精度实现的性能、WIT 转换与常量资源上限；
2. Decimal 的精度、舍入上下文和序列化规范；
3. 固定宽度整数的正式名称及公开程度；
4. 泛型约束、接口/trait、方差和编译策略；
5. 资源借用是否进入表层语言，以及需要怎样的生命周期推导；
6. 闭包循环与自动内存管理的最低实现模型；
7. 效果集合的正式分类、粒度和 handler 是否必要；
8. `Future` 的启动细节、取消观察点和 Component async ABI 映射；
9. 契约表达式子集、静态证明器和运行时检查成本；
10. WIT 接口版本兼容、adapter 生成和 WASI 基线；
11. 语义索引与 AI 查询协议的 JSON schema；
12. 候选 B、C 如何用更低歧义的语法表达本文语义。

这些问题必须使用 RFC、原型或量化实验解决，不能由 Rust 类型、Wasm 限制或候选 A 的写法自动决定。

## 18. 下一步验证

1. 用本文条目逐个重审 10 个代表性程序，标出不一致与无法表达处；
2. 为每条 P0 语义编写最小成功例和最小失败例；
3. 根据同一语义设计候选 B、C，不改变程序行为；
4. 比较三套语法的 AI 首次生成成功率、理解 token、诊断 token 和单轮修复率；
5. 单独编写诊断协议 v0 与语义索引 JSON v0；
6. 对 `Int`、资源、Future 和 WIT 映射做 Rust/Wasm Component 技术原型；
7. 只有关键 P0 语义通过验证后，才建立正式编译器前端。
