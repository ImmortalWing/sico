# Sico 开发与架构文档

> 状态：开发草案
> 更新日期：2026-07-14
> 上位文档：[DIRECTION.md](./DIRECTION.md)

本文档说明 Sico 应如何从语言设计进入可验证的工程实现。它描述编译器、WebAssembly Component 后端、应用包、运行时、Host、安全模型、测试方法和阶段目标。

本文档不负责提前确定尚未讨论完成的表层语法。若本文档与 `DIRECTION.md` 冲突，以 `DIRECTION.md` 中已经确认的方向为准。

## 1. 文档约定

本文使用三种决策状态：

- **已确认**：已经形成共识，实现时应遵守；
- **工作方案**：当前推荐做法，可以通过原型和数据调整；
- **开放问题**：尚未决定，不得在实现中悄悄固化。

任何工作方案升级为已确认决策前，应至少具备代表性示例、失败案例、替代方案比较和可测试的验收标准。

## 2. 项目目标

### 2.1 已确认目标

Sico 是一门面向 AI 理解、生成、检查和修复代码的正规编程语言。

工程目标包括：

- 让 AI 尽量难以生成语法非法或结构含糊的程序；
- 通过强静态检查，在运行前阻止尽可能多的真实错误；
- 产生清晰、简短、稳定、低 token 的诊断；
- 让 AI 能够根据单轮结构化诊断完成局部修复；
- 保持源码语义确定、可复现，不依赖模型临场解释；
- 使用 Rust 实现第一代编译器、Runtime 和 Host 核心；
- 使用 WebAssembly Component 作为正式执行与分发格式；
- 使用 `.sapp` 作为跨平台单文件应用包；
- 首先建立 Android 和桌面端的原生 Sico 应用生态；
- 不依赖 JavaScript 或 TypeScript；
- 不重复发明 WebAssembly Component 已提供的执行格式、Canonical ABI、组件组合和沙箱基础。

### 2.2 当前非目标

当前阶段不做：

- 纯自然语言编程；
- JavaScript、TypeScript、HTML 或某个前端框架的包装层；
- 为了演示而设计一套未经论证的玩具语法；
- 自研公开字节码、通用虚拟机或跨语言 ABI；
- 浏览器 DOM 后端；
- 第一阶段自举；
- 在语言语义未稳定前追求极致机器码优化；
- 在安全模型未完成前加载不可信远程应用。

## 3. 总体架构

### 3.1 编译与分发流程

```text
Sico 源码与资源
       ↓
源码管理与解析
       ↓
AST
       ↓
名称解析、类型检查、效果与能力检查
       ↓
带类型的合法程序
       ↓
Sico IR
       ↓
WebAssembly Core Module
       ↓
WebAssembly Component + WIT 接口
       ↓
.sapp 应用包
       ↓
Sico Host / Sico Runtime
       ↓
原生窗口、图形、输入、存储、网络与系统能力
```

### 3.2 核心边界

| 层 | 负责 | 不负责 |
|---|---|---|
| Sico 语言 | 类型、值、控制流、错误、副作用和能力语义 | UI 框架和平台实现 |
| 编译器前端 | 解析、AST、名称解析、类型检查和诊断 | 执行不可信应用 |
| Sico IR | 统一高级语义并支持确定性代码生成 | 公开分发和跨版本 ABI |
| Wasm Component 后端 | 生成 Core Wasm、Component 和 WIT 绑定 | 定义 Sico 表层语法 |
| `.sapp` | 封装 Component、资源、清单、权限和签名 | 定义代码执行语义 |
| Sico Runtime | 验证、实例化、调度、权限和宿主接口 | 编辑源码 |
| Sico Host | 文件关联、应用展示、授权和生命周期 | 编译语言源码 |

### 3.3 不可破坏的边界

- UI、存储、网络等平台接口不得成为核心语法的特殊规则；
- Sico IR 不作为长期兼容的公开格式；
- `.sapp` 不携带 Sico IR，只携带正式 Component 载荷；
- 应用不能绕过 Runtime 直接访问操作系统；
- 编译器不能为了后端方便而改变已经定义的 Sico 语义；
- Runtime 不允许通过隐式权限补全让非法应用“凑合运行”。

## 4. Rust 工具链

### 4.1 已确认范围

以下组件使用 Rust 实现：

- `sico` 命令行程序；
- 源码管理、词法分析和解析；
- AST 与语义模型；
- 名称解析和类型检查；
- 诊断引擎；
- Sico IR；
- Wasm 与 Component 代码生成；
- `.sapp` 打包、校验和签名工具；
- Sico Runtime；
- Sico Host 的共享核心；
- 语言服务器核心。

平台外壳可以使用必要的原生平台代码，但必须保持薄层，并通过明确接口连接 Rust 核心。

### 4.2 工作区结构提案

早期应使用一个 Cargo workspace。逻辑边界建议如下，但在代码量较小时可以合并 crate，避免过度拆分：

```text
sico/
├── crates/
│   ├── sico-source/       源文件、位置、跨度和增量文本
│   ├── sico-syntax/       词法、解析和无损语法树
│   ├── sico-ast/          稳定的语义 AST
│   ├── sico-diagnostics/  错误编号、渲染和 JSON 协议
│   ├── sico-semantics/    名称解析、类型、效果和能力检查
│   ├── sico-index/        符号、调用、状态、契约和影响索引
│   ├── sico-ir/           Sico IR 定义、验证和转换
│   ├── sico-wasm/         Core Wasm 代码生成
│   ├── sico-component/    WIT、Canonical ABI 和组件封装
│   ├── sico-package/      .sapp 打包、哈希和签名
│   ├── sico-runtime/      Runtime、权限和宿主能力
│   ├── sico-cli/          sico 命令行入口
│   └── sico-lsp/          语言服务器
├── apps/
│   ├── host-desktop/      Sico Desktop Host
│   └── host-android/      Sico Android Host
├── wit/                   Sico 平台 WIT 接口
├── spec/                  语言规范和语义测试
├── tests/                 端到端、兼容性和安全测试
├── examples/              代表性 Sico 程序
└── docs/                  RFC、ADR 和开发文档
```

该目录只是工作方案。第一阶段可以先使用较少 crate，等依赖边界稳定后再拆分。

## 5. 编译器前端

### 5.1 源码模型

源码基础设施必须统一管理：

- 文件 ID；
- UTF-8 文本；
- 字节偏移；
- 行列映射；
- 节点跨度；
- 文件版本；
- 原始文件名与规范化路径。

所有诊断、AST 节点和后续 IR 来源信息都引用统一跨度类型，禁止各模块自行计算行列。

Unicode 标识符是否允许、如何规范化以及是否允许中文标识符属于开放问题。无论最终选择如何，编译器必须识别全角结构符号、不可见字符和易混淆字符，并提供短诊断。

### 5.2 语法树与 AST

工作方案采用两层结构：

1. **无损语法树**：保留空白、注释和错误节点，服务格式化、编辑器和增量解析；
2. **语义 AST**：只保留语言结构，服务名称解析、类型检查和诊断。

AST 节点至少包含：

- 稳定节点类别；
- 必要字段；
- 源码跨度；
- 可选的语法节点引用；
- 编译期生成节点的来源链。

不得把后端 Wasm 类型直接放入 AST。AST 描述 Sico，而不是目标平台。

### 5.3 解析器要求

解析器必须满足：

- 语法确定且无歧义；
- 单个错误能够局部恢复；
- 错误节点仍可进入后续编辑器流程；
- 不因一个缺失结束符产生大量级联错误；
- 相同错误始终产生相同错误编号和主要跨度；
- 支持错误数量上限；
- 支持规范格式化；
- 可以对代表性错误语料进行模糊测试。

在选择括号、缩进或显式结束符前，应为同一批代表性程序设计多个候选语法，并测量：

- AI 首次生成解析成功率；
- 缺失符号后的级联错误数量；
- 最远配对距离；
- 规范格式化后的 token 数；
- 单轮诊断修复成功率。

### 5.4 名称解析

名称解析负责：

- 作用域建立；
- 定义和引用绑定；
- 重复定义检测；
- 不存在名称检测；
- 导入与模块边界；
- 可见性；
- 标准库和能力接口名称。

名称解析结果使用内部符号 ID，后续阶段不应反复依赖字符串查找。

名称错误应优先给出唯一、可信的候选；如果候选不够确定，则不输出长列表。

## 6. 类型、错误、效果与能力检查

### 6.1 已确认原则

- 强静态类型；
- 支持局部类型推导；
- 禁止隐式数字、字符串和布尔转换；
- 普通类型不隐式包含 `null`；
- 使用显式类型表达“可能不存在”和“可能失败”；
- 模式匹配必须完整；
- 变量默认不可变；
- 副作用和平台能力可检查。

### 6.2 类型检查输出

类型检查阶段不只返回成功或失败，还应产出：

- 每个表达式的确定类型；
- 每个名称引用对应的定义；
- 泛型实例化结果；
- 隐式但确定的语言操作；
- 控制流收窄结果；
- 效果集合；
- 所需能力集合；
- 状态读取和写入集合；
- 静态调用关系；
- 已验证的契约信息；
- 错误根因和传播来源。

只有通过类型、效果和能力检查的程序才能进入 Sico IR。

### 6.3 错误模型开放问题

以下内容需要通过完整示例决定：

- 使用 `Option<T>`、`Result<T, E>` 还是其他名称；
- 是否存在不可恢复异常；
- 错误传播是否有专用语法；
- 错误类型采用开放还是封闭集合；
- 跨 Component 边界如何映射 WIT `result`；
- Runtime 崩溃与业务失败如何严格区分。

在这些问题确认前，不实现表层错误语法。

### 6.4 效果与能力

语言级效果描述“函数做了什么”，平台能力描述“应用被允许做什么”。二者相关但不能混为一谈。

示例概念：

```text
函数效果：读取存储、发起网络请求、显示窗口
应用能力：storage.read、network.connect、ui.window
```

编译器应汇总入口可达调用图中的能力需求，并与应用清单对比：

- 代码使用但清单未声明：编译错误；
- 清单声明但代码未使用：默认警告或打包检查；
- Runtime 未提供：加载失败；
- 用户未授权：结构化运行时权限错误。

具体效果系统仍是开放问题，但能力检查必须最终能够映射到 Component imports。

## 7. 诊断系统

### 7.1 设计目标

诊断必须满足：

- 短：默认输出不解释背景知识；
- 准：主要跨度指向真正错误位置；
- 稳定：编号和 JSON 字段受兼容性约束；
- 可修复：必要时只提供一条首选修复；
- 低 token：不重复源码和上下文；
- 可展开：详细原因通过独立查询获取。

### 7.2 默认文本格式

```text
E021 user.sico:18:10 expected User, found Option<User>
hint: handle none before using the value
```

### 7.3 机器协议工作方案

```json
{
  "version": 1,
  "diagnostics": [
    {
      "code": "E021",
      "severity": "error",
      "file": "user.sico",
      "range": [18, 10, 18, 26],
      "message": "expected User, found Option<User>",
      "expected": "User",
      "found": "Option<User>",
      "hint": "handle none before using the value"
    }
  ]
}
```

字段存在时必须含义稳定；不适用的字段应省略，而不是填入无意义空值。

### 7.4 诊断命令

计划提供：

```text
sico check                 默认简短文本
sico check --json          机器格式
sico check --max-errors 1  限制上下文占用
sico explain E021          按需查看详细解释
```

命令名称是工作方案，但“默认简短、机器模式、错误上限、按需展开”是已确认能力。

### 7.5 级联抑制

诊断引擎应记录错误依赖关系。例如未解析名称导致类型未知时，默认只报告未解析名称，不继续报告由未知类型导致的运算符、返回值和泛型错误。

每类错误测试必须同时验证：

- 错误编号；
- 主要跨度；
- 默认文本长度；
- JSON 字段；
- 是否错误地产生级联诊断；
- AI 是否能根据诊断完成局部修复。

## 8. Sico IR

### 8.1 定位

Sico IR 是编译器内部、带类型、可验证的中间表示。它负责将多种高级 Sico 结构统一为少量明确操作，再生成 Wasm。

Sico IR 不承担：

- 跨版本公开兼容；
- 第三方应用分发；
- 运行时动态加载协议；
- 取代 WebAssembly；
- 成为用户手写语言。

### 8.2 设计要求

Sico IR 应具备：

- 所有值类型明确；
- 控制流明确；
- 调用目标和调用约定明确；
- 错误路径明确；
- 效果和能力调用明确；
- 源码来源可追踪；
- 确定性序列化，便于快照测试；
- 独立验证器，拒绝编译器内部产生的非法 IR；
- 能自然降低到 Wasm 的块、函数、内存和组件接口。

### 8.3 工作方案

初期采用函数、基本块和块参数组成的强类型控制流 IR。是否使用完整 SSA、如何表示闭包、异步、资源和内存管理，需要通过 Wasm Component 原型决定。

每个从 AST 到 IR 的转换必须具有语义测试；优化前后必须保持可观察行为一致。

### 8.4 初期优化边界

第一阶段只实现低风险优化：

- 常量折叠；
- 不可达代码移除；
- 明确无副作用的无用值移除；
- 简单控制流归一化。

在语义和调试信息稳定前，不追求复杂跨函数优化。

## 9. WebAssembly Component 后端

### 9.1 已确认决策

- Sico 编译到 WebAssembly Core Module；
- Core Module 封装为 WebAssembly Component；
- 组件接口使用 WIT；
- 数据跨界遵循 Canonical ABI；
- 通用系统能力优先复用 WASI；
- UI 和 Sico 特有平台能力通过独立 WIT 包提供；
- `.sapp` 的正式代码载荷是 Component，而不是 Sico IR。

### 9.2 后端职责

后端负责：

- 将 Sico 类型映射为 Wasm Core 与 WIT 类型；
- 生成函数、控制流和内存访问；
- 生成或嵌入必要运行时支持；
- 生成 imports 和 exports；
- 验证 Component 世界是否完整；
- 保留源码映射和调试元数据；
- 产生确定性二进制；
- 调用标准工具验证生成结果。

### 9.3 WIT 平台包工作方案

首批接口按能力拆分，避免单个“万能宿主接口”：

```text
sico:app        应用入口、挂起、恢复和退出
sico:ui         窗口、视图、布局和渲染提交
sico:input      指针、键盘、触摸和文本输入
sico:storage    应用隔离存储
sico:network    受权限控制的网络访问
sico:clipboard  剪贴板
sico:dialog     文件选择、消息和权限交互
sico:log        结构化日志
```

名称和具体接口尚未定稿。每个接口必须满足：

- 能力粒度足够细；
- 可以独立版本化；
- 不暴露平台原生指针；
- 不要求共享线性内存；
- 错误通过明确类型返回；
- 异步语义与选定 WASI 基线一致；
- Android 与桌面端能够实现相同核心语义。

### 9.4 版本兼容

组件清单和 WIT 接口必须使用显式版本。Runtime 加载时执行：

1. 检查 `.sapp` 格式版本；
2. 验证 Component；
3. 读取所需 WIT worlds；
4. 检查 Runtime 是否提供兼容接口；
5. 检查清单能力是否覆盖 imports；
6. 请求用户授权；
7. 实例化应用。

WASI 基线版本仍是开放问题。选型时必须验证 Runtime 支持度、Android 可用性、异步模型和未来迁移路径。

## 10. `.sapp` 应用包

### 10.1 定位

`.sapp` 是确定、可验证、可签名的单文件应用容器。它负责应用级封装，不定义 Component 内部执行语义。

### 10.2 逻辑内容

```text
example.sapp
├── manifest              包格式、应用 ID、版本、入口和权限
├── app.component.wasm    WebAssembly Component
├── assets/               图片、字体及其他只读资源
├── metadata/             可选构建与调试信息
└── signature             哈希与发布者签名
```

实际归档格式、清单编码和签名算法属于开放问题。无论选择哪种格式，都必须支持：

- 确定性构建；
- 流式校验；
- 路径规范化；
- 防目录穿越；
- 单文件大小与解压大小限制；
- 内容哈希；
- 可选或强制发布者签名策略；
- 不解压全部内容即可读取清单；
- 资源按需读取。

### 10.3 应用身份

应用身份不能只依赖显示名称。工作方案使用稳定应用 ID、发布者身份和签名共同确定身份，并以此隔离：

- 存储目录；
- 权限记录；
- 更新来源；
- 网络策略；
- 崩溃和日志数据。

开发模式可以允许未签名本地包，但必须有明显标记，且不能继承正式发布包的权限和身份。

### 10.4 两种发布形式

```text
sico build --package  → example.sapp
sico build --native   → Component + 精简 Runtime + 平台安装包
```

命令参数属于工作方案。两种形式应运行同一 Component，并通过相同 WIT 接口获得能力，避免产生语义不同的双后端。

## 11. Sico Runtime

### 11.1 职责

Runtime 负责：

- 读取并验证 `.sapp`；
- 验证 WebAssembly Component；
- 提供 WASI 与 Sico WIT 宿主接口；
- 应用沙箱；
- 权限和能力检查；
- 任务、异步操作和事件调度；
- 应用隔离存储；
- 资源加载；
- 结构化日志和崩溃报告；
- 生命周期管理；
- 资源限额。

### 11.2 Runtime 不负责

- 猜测应用缺失的权限；
- 动态修复非法 Component；
- 允许应用访问任意原生符号；
- 将不同应用的内存或存储隐式共享；
- 为兼容错误应用而保留未定义行为。

### 11.3 执行引擎选型

具体 Component Model 执行引擎尚未确定。选型原型必须比较：

- Component Model 与目标 WASI 版本支持；
- Android、Windows、macOS、Linux 支持；
- 解释、JIT 与 AOT 能力；
- Runtime 体积；
- 冷启动时间；
- 内存占用；
- 异步宿主接口；
- 资源限额与中断机制；
- 调试和崩溃定位；
- 安全维护记录；
- Rust 嵌入 API 稳定性。

在基准和最小端到端原型完成前，不在规范中绑定具体引擎。

## 12. Sico Host

### 12.1 用户体验目标

用户只安装一次 Sico Host，之后可以：

- 双击本地 `.sapp`；
- 从分享链接下载并打开应用；
- 通过自定义协议或二维码启动；
- 查看应用来源、版本和权限；
- 授权或拒绝敏感能力；
- 清除单个应用的数据；
- 在不影响其他应用的情况下关闭失控应用。

### 12.2 平台结构

```text
桌面/Android 平台外壳
          ↓
Sico Host 共享核心
          ↓
Sico Runtime
          ↓
Component 执行引擎
```

平台外壳负责文件关联、窗口入口、系统分享和安装体验；共享核心负责包管理、权限、Runtime 和应用生命周期。

### 12.3 UI 边界

UI 是平台 SDK，不是核心语言语法。当前只确定以下要求：

- 同一应用在 Android 和桌面端保持行为语义一致；
- 允许平台适配布局和输入方式；
- UI 调用通过 WIT 能力接口；
- Runtime 能够限制 UI 资源和事件频率；
- 应用不能获得原生窗口或控件指针；
- 可访问性、文本输入和国际化不能作为后期补丁。

采用保留模式 UI、即时模式 UI、自绘渲染或原生控件映射仍是开放问题。

## 13. 安全模型

### 13.1 威胁假设

Runtime 必须假设 `.sapp` 可能：

- 来自未知发布者；
- 包含恶意或被篡改的 Component；
- 请求过多权限；
- 消耗无限 CPU、内存、存储或网络；
- 构造恶意资源文件；
- 尝试读取其他应用数据；
- 尝试伪造更新或应用身份；
- 利用 Runtime、解码器或图形库漏洞。

### 13.2 默认安全规则

- 默认无权限；
- 能力必须在清单和 Component imports 中同时出现；
- Runtime 只提供已授权 imports；
- 每个应用使用独立存储命名空间；
- 默认禁止原生 FFI；
- 限制 CPU、内存、文件、解压和网络资源；
- 所有包路径经过规范化；
- 安装和更新执行哈希与签名校验；
- 权限升级必须重新确认；
- 调试模式与正式模式严格隔离。

### 13.3 安全测试

必须包含：

- 包解析模糊测试；
- Wasm/Component 验证失败测试；
- 权限绕过测试；
- 存储隔离测试；
- 资源耗尽测试；
- 恶意图片、字体和压缩包测试；
- 签名替换和降级攻击测试；
- Runtime 崩溃恢复测试。

安全边界未通过测试前，Host 只能运行本地开发包，不开放远程生态。

## 14. AI 与 Vibe Coding 工作流

### 14.1 基本循环

```text
用户描述需求
    ↓
AI 修改 Sico 源码
    ↓
sico check --json --max-errors 1
    ↓
AI 根据单条根因诊断局部修复
    ↓
sico test
    ↓
sico build
```

编译器是 AI 的确定性约束器，不负责替代模型理解用户需求。

### 14.2 AI 友好协议

工具需要提供：

- 稳定 JSON 诊断；
- 规范格式化；
- 精确源码跨度；
- 符号定义和引用查询；
- 类型与能力查询；
- 最小修改后的快速增量检查；
- 错误数量限制；
- 按编号展开诊断；
- 不依赖终端颜色或自然语言解析的机器接口。

### 14.3 评测数据集

项目应维护独立评测集，包含：

- 自然语言需求；
- 期望的程序行为；
- AI 首次生成结果；
- 编译诊断；
- AI 修复结果；
- 测试结果；
- token 消耗；
- 修改范围。

核心指标：

- 首次解析成功率；
- 首次类型检查成功率；
- 单轮修复成功率；
- 平均诊断 token；
- 平均修改行数或 AST 节点数；
- 修复是否引入新的无关变化；
- 相同提示重复生成时的结构稳定性。

语言特性必须证明能改善这些指标，不能只凭语法审美加入。

### 14.4 AI 理解模型

Sico 对 AI 可理解性的目标不是让源码“看起来像自然语言”，而是让程序能够被快速、分层、确定地概括。

AI 理解一个功能时需要回答六类问题：

1. **职责**：模块或函数对外提供什么；
2. **输入输出**：接受和返回哪些领域值；
3. **约束**：调用前需要满足什么，成功后保证什么；
4. **状态**：读取、创建、修改和删除哪些状态；
5. **外部行为**：产生哪些效果、错误和平台能力调用；
6. **影响范围**：哪些调用者、测试、组件和应用行为依赖它。

编译器必须把这些信息建模为正式语义，而不是要求 AI 从任意注释、命名习惯和跨文件代码中猜测。

### 14.5 源码层优化原则

以下是语言设计的工作方案：

- 模块依赖显式列出，避免隐式导入和不受控通配导入；
- 公共定义的签名能够独立阅读；
- 领域概念使用领域类型，而不是大量无含义的字符串和数字；
- 可预期错误出现在返回类型中；
- 副作用和所需能力可以从定义处查询；
- 状态默认不可变，修改点显式且可搜索；
- 实际调用目标能够静态解析，动态派发范围可枚举；
- 控制流保持结构化，限制非局部跳转和隐藏控制流；
- 模式匹配完整，状态分支不会隐藏在默认分支中；
- 模块成员按统一顺序格式化；
- 一个概念只有一种规范表达，减少 AI 在等价写法之间选择；
- 宏、反射和元编程不得破坏静态索引和影响分析。

这不意味着所有高级能力都永久禁止，而是任何会扩大隐藏语义范围的能力都必须证明：编译器仍能给出准确摘要、调用关系和影响范围。

### 14.6 形式化契约工作方案

公共函数和关键状态转换应支持机器可读契约。下面只是语义示意，不是最终语法：

```text
transfer(from: Account, to: Account, amount: Money)
  returns Result<Receipt, TransferError>
  requires amount > 0
  requires from.balance >= amount
  ensures from.balance == old(from.balance) - amount
  ensures to.balance == old(to.balance) + amount
  effects storage.write
  errors insufficient_balance, account_closed
```

AI 不必先阅读函数体，就能知道：

- 功能是账户间转账；
- 输入和返回类型；
- 调用条件；
- 成功后的状态变化；
- 可能失败的原因；
- 会写入存储。

契约分为三个层次：

- **类型契约**：参数、返回值、错误、效果和能力，必须静态检查；
- **逻辑契约**：前置条件、后置条件和不变量，在编译期证明、运行时检查或测试生成之间选择；
- **业务说明**：可选自然语言摘要，帮助解释意图，但不承担正确性。

具体支持到哪一级逻辑证明属于开放问题。第一阶段可以从可静态提取的类型、错误、效果、能力和简单前置条件开始。

### 14.7 语义索引

编译器应为项目生成内存中或缓存化的 **Sico Semantic Index**。它不是新的源码格式，也不进入 `.sapp` 的运行载荷。

索引至少记录：

- 模块、类型、函数、常量和状态的稳定内部 ID；
- 公共和私有可见性；
- 类型签名；
- 契约；
- 效果和能力；
- 可能错误；
- 状态读取和写入；
- 调用者和被调用者；
- 类型实现和模式覆盖关系；
- 相关测试和示例；
- 源码位置；
- 定义的语义哈希。

语义哈希用于判断一次修改是否真正改变公开行为。格式化、注释和不影响语义的重排不应导致公共语义哈希变化。

模块概要的机器输出可以类似：

```json
{
  "module": "payments",
  "purpose": "payment orchestration",
  "exports": ["create_payment", "refund"],
  "state": ["Payment"],
  "effects": ["storage.write", "network.payment_gateway"],
  "capabilities": ["storage", "network"],
  "errors": ["declined", "gateway_unavailable"],
  "depends_on": ["accounts", "ledger"],
  "tests": ["payment_success", "payment_declined"]
}
```

字段和名称是工作方案。正式协议必须使用稳定 ID、版本和可省略字段，避免自然语言重复占用上下文。

### 14.8 面向 AI 的语义查询

除了 `check`，工具链应提供按需理解程序的接口。计划能力包括：

```text
sico outline <module>       返回模块结构和公开职责
sico describe <symbol>      返回签名、契约、效果、错误和状态
sico callers <symbol>       返回直接或传递调用者
sico slice <symbol>         返回理解该符号所需的最小程序切片
sico impact <symbol/change> 返回修改影响的符号、测试、能力和组件
sico flow <state/type>      返回状态转换或数据流
```

这些命令名称尚未定稿，但对应能力属于 AI 工具链目标。

所有查询都应支持短文本和结构化 JSON，并允许限制：

- 深度；
- 结果数量；
- token 预算；
- 是否包含私有实现；
- 是否包含源码片段；
- 是否展开传递依赖。

### 14.9 AI 阅读协议

AI 修改已有项目时，推荐流程是：

```text
1. outline   获取项目或模块地图
2. describe  理解目标符号的正式行为
3. slice     只读取必要实现和依赖
4. impact    修改前计算潜在影响
5. edit      进行局部源码修改
6. check     获取一个根因诊断
7. test      验证行为和契约
8. impact    确认没有意外扩大变化范围
```

这比让 AI 每次通读整个仓库更快，也更不容易遗漏隐藏依赖。

### 14.10 状态与流程可理解性

应用逻辑中最难理解的通常不是算术表达式，而是状态如何变化。Sico 应优先让状态机显式化：

- 状态使用封闭枚举或代数数据类型表达；
- 合法转换可以静态枚举；
- 转换函数明确输入状态、输出状态、错误和效果；
- 非法转换成为编译错误或明确的结果类型；
- 编译器能够生成状态转换图；
- UI、存储和网络事件对状态的影响可追踪。

状态机的具体表层语法仍是开放问题。无论语法如何，语义索引必须能够回答“这个状态从哪里来、可以去哪里、谁能修改”。

### 14.11 可理解性评测

除生成和修复指标外，应建立理解能力评测，要求 AI 在限定上下文中回答：

- 模块主要功能是什么；
- 某个函数会修改什么；
- 某种错误通过哪些路径产生；
- 某个状态有哪些合法转换；
- 删除或修改某符号会影响什么；
- 完成指定变更最少需要阅读哪些定义。

记录：

- 正确率；
- 所需输入 token；
- 查询次数；
- 是否读取无关文件；
- 漏掉的依赖数量；
- 修改后的意外行为变化。

语言和工具链优化必须同时改善“生成、诊断、修复、理解”四个维度。

## 15. CLI 与开发工具

### 15.1 计划命令

```text
sico check      解析并检查项目
sico fmt        生成唯一规范格式
sico test       运行语言和应用测试
sico build      生成 Component 与 .sapp
sico run        在开发 Runtime 中运行
sico inspect    查看包、imports、exports 和权限
sico explain    展开错误编号
```

实际参数在 CLI RFC 中确定。所有命令应支持机器可读模式，并使用一致的退出码规则。

### 15.2 语言服务器

语言服务器使用 Rust 实现，提供：

- 实时诊断；
- 跳转定义；
- 查找引用；
- 重命名；
- 类型悬停；
- 规范格式化；
- 能力与效果提示；
- 安全、确定的代码操作。

编辑器插件只是 LSP 客户端，不承载语言语义。插件可以按编辑器要求使用其生态技术，但编译与检查结果必须来自 Rust 语言服务器。

## 16. 测试策略

### 16.1 测试层次

| 层 | 主要测试 |
|---|---|
| 源码与解析 | lexer/parser 单元测试、错误恢复、模糊测试 |
| AST | 结构快照、跨度、注释和格式化往返 |
| 语义 | 名称、类型、完整匹配、效果和能力测试 |
| 诊断 | 编号、跨度、短文本、JSON 和级联抑制快照 |
| IR | 验证器、转换前后语义、确定性快照 |
| Wasm | 二进制验证、接口映射和源码映射 |
| Component | WIT world、imports/exports 和组合测试 |
| `.sapp` | 确定性、签名、路径和资源限制测试 |
| Runtime | 权限、隔离、资源限额和崩溃恢复 |
| Host | 文件打开、生命周期、升级和跨平台行为 |
| AI | 生成成功率、单轮修复率和 token 指标 |

### 16.2 编译成功测试

每个语言特性至少包含：

- 最小成功程序；
- 多个特性组合程序；
- 格式化后的稳定输出；
- AST 与 IR 快照；
- Component 验证；
- Runtime 行为断言。

### 16.3 编译失败测试

每条错误编号至少包含：

- 最小触发案例；
- 主要跨度；
- 默认短文本；
- JSON 输出；
- 不应出现的级联错误；
- 修复后的成功案例。

### 16.4 跨平台一致性

同一 `.sapp` 在所有支持平台上应通过统一行为测试。允许 UI 外观遵循平台习惯，但类型、错误、存储隔离、权限和业务逻辑语义必须一致。

## 17. 构建、CI 与发布

### 17.1 每次提交检查

计划执行：

- Rust 格式检查；
- 静态检查；
- 单元与集成测试；
- 诊断和 IR 快照；
- Wasm/Component 验证；
- `git diff --check`；
- 文档链接和示例验证。

### 17.2 定期检查

- parser 和 package fuzzing；
- 多平台 Runtime 测试；
- 性能基准；
- 依赖漏洞与许可证检查；
- AI 生成与修复基准；
- 可复现构建验证。

### 17.3 版本策略

以下版本分别管理，禁止混为一个数字：

- Sico 语言版本；
- 编译器版本；
- `.sapp` 包格式版本；
- Sico WIT 平台接口版本；
- Runtime/Host 版本；
- WASI 基线版本。

兼容承诺应在 1.0 前通过明确实验版本表达，不能默认永久兼容早期草案。

## 18. 开发流程与决策管理

### 18.1 语言特性流程

每个语言特性依次经过：

1. 提出要解决的 AI 或软件正确性问题；
2. 提供代表性成功程序；
3. 提供常见失败程序；
4. 比较现有语言方案；
5. 定义静态语义；
6. 定义短诊断；
7. 定义 AST 与 IR 形态；
8. 定义 Wasm/Component 映射；
9. 添加一致性测试；
10. 通过 AI 生成和修复评测。

缺少任何一项时，特性不得视为稳定。

### 18.2 ADR 与 RFC

- **ADR** 记录工程架构决策，例如 Runtime 引擎和包格式；
- **RFC** 记录语言、标准库、WIT 接口和兼容性提案；
- 被替代的决策保留历史并标记状态，不直接删除；
- 每个决策说明背景、候选方案、取舍和验证证据。

## 19. 分阶段开发计划

### M0：设计基线

交付：

- 语言目标和非目标；
- 至少 10 个代表性完整程序；
- AI 常见错误分类；
- AI 程序理解任务和基线数据；
- 三种候选语法比较；
- 类型、错误、效果和能力模型草案；
- 形式化契约和语义索引草案；
- 诊断协议 v0；
- Wasm Component 最小技术原型；
- Runtime 引擎对比报告。

退出条件：尚未确定的关键语义不再通过编译器代码被隐式决定。

### M1：前端与诊断

交付：

- Rust workspace；
- 源码与跨度基础设施；
- 解析器和错误恢复；
- AST；
- `sico check`；
- 基础模块 outline；
- 文本与 JSON 诊断；
- 规范格式化器；
- parser fuzzing。

退出条件：代表性程序可稳定解析，错误语料不会产生不可控级联诊断。

### M2：静态语义

交付：

- 名称解析；
- 基础类型系统；
- 局部类型推导；
- 显式可选值和错误；
- 完整模式匹配；
- 不可变检查；
- 初步效果和能力检查。
- 初步语义索引、符号 describe 和调用关系查询。

退出条件：核心示例中的非法程序能在进入后端前被准确拒绝。

### M3：IR 与 Component

交付：

- Sico IR 和验证器；
- AST 到 IR 降低；
- Core Wasm 代码生成；
- Component 封装；
- 最小 WIT 应用入口；
- 端到端命令行程序执行。

退出条件：同一输入产生确定的 Component，并在选定 Runtime 中通过行为测试。

### M4：`.sapp` 与 Runtime

交付：

- `.sapp` v0；
- 清单、资源、哈希和开发签名；
- Runtime 验证和加载流程；
- 隔离存储；
- 权限模型；
- CPU、内存和资源限额；
- `sico build`、`run` 和 `inspect`。

退出条件：不可信测试包无法绕过声明能力访问宿主资源。

### M5：Sico Desktop Host

交付：

- Sico Desktop Host；
- `.sapp` 文件关联；
- 应用信息和权限界面；
- 生命周期与崩溃隔离；
- 首个最小 UI 能力接口；
- Windows、macOS、Linux 行为测试。

退出条件：一个 `.sapp` 可在支持的桌面系统上获得一致核心行为。

### M6：Sico Android Host

交付：

- Sico Android Host；
- 文件、链接和分享入口；
- 触摸、文本输入和生命周期适配；
- Android 权限映射；
- 与桌面端共享的应用行为测试。

退出条件：同一 `.sapp` 无需重新编译即可在 Android 和桌面端运行。

### M7：生态与发布

交付：

- 包注册和发现方案；
- 发布者身份和签名策略；
- 安全更新机制；
- 标准库稳定边界；
- 完整 LSP；
- AI 工具协议；
- 真实应用和第三方组件试点。

退出条件：外部开发者能够在不修改编译器和 Runtime 的情况下开发、发布和运行应用。

## 20. 性能与质量指标

第一阶段不提前写死数值，但从首个原型开始持续记录：

- 冷编译时间；
- 增量检查时间；
- 峰值编译内存；
- Component 大小；
- `.sapp` 大小；
- Runtime 和 Host 安装体积；
- 应用冷启动时间；
- 空闲和运行内存；
- 宿主调用开销；
- AI 首次通过率；
- 单轮修复率；
- 诊断 token；
- 模块功能概括正确率；
- 理解目标功能所需 token；
- 修改影响范围识别准确率；
- 最小程序切片相对完整仓库的压缩比例；
- Runtime 崩溃和沙箱逃逸数量。

在获得真实原型数据后，通过 ADR 确定性能预算。

## 21. 当前开放问题

语言层：

- 表层语法和文件组织；
- 中文标识符与 Unicode 规范化；
- 名义类型、结构类型或混合模型；
- 泛型与多态；
- 所有权、GC 或其他内存管理模型；
- 错误与不可恢复异常；
- 效果系统；
- 并发和异步；
- 模块、依赖和版本规则。

后端层：

- Sico IR 的具体控制流形式；
- 闭包、资源、异步和内存如何降低到 Wasm；
- Component 执行引擎；
- WASI 基线和迁移策略；
- 是否需要后期直接原生机器码后端。

平台层：

- `.sapp` 物理归档和清单编码；
- 签名与发布者身份体系；
- UI 模型和渲染后端；
- 标准库与平台 SDK 边界；
- 更新、撤销和生态分发方式；
- Android 与桌面端最低支持版本。

这些问题必须通过原型、测试和数据决定，不能因某门现有语言或框架的习惯直接继承。

## 22. 开始实现前的立即任务

在创建 Rust workspace 前，应先完成：

1. 建立 AI 常见错误分类文档；
2. 建立 AI 对现有程序进行概括、依赖追踪和影响分析的评测任务；
3. 编写 10 个覆盖 CLI、服务、数据处理和 UI 的代表性 Sico 程序草案；
4. 为相同程序设计至少三套候选语法；
5. 比较各语法在 AI 生成、修复和理解上的 token 与正确率；
6. 定义最小值与类型语义；
7. 定义形式化契约、效果、能力和语义索引草案；
8. 定义诊断协议 v0 和错误编号分区；
9. 用 Rust 单独验证“生成 Component → Runtime 加载 → WIT 宿主调用”的最小链路；
10. 比较 Component Runtime 在桌面和 Android 上的可行性；
11. 根据结果确认 M1 的工程结构。

完成以上任务后，才开始正式编写 Sico 编译器前端。
