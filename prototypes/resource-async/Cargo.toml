[package]
name = "sico-resource-async-prototype"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"

[dev-dependencies]
wit-parser = "0.253.0"

[profile.release]
overflow-checks = true
lto = "thin"
